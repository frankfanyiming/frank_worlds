# 柯南建筑深化证据

最终实机截图位于 `pass04/`，包含 22 个实际 Godot Compatibility 昼夜视角。`pass01`～`pass03` 保留迭代过程，不是最终效果。

最终物理路线报告为 `routes03/physical-routes.json`：41 项检查、295 个实际移动分段、0 失败。灯强度最后修订仅改变照明，控制器与物理几何没有变化。

`asset-manifest.json` 记录源模型和网页资产 SHA，以及 unchanged 的三个角色 SHA；网页包导出/发布由主任务处理。

参见 [制作方法与验收说明](../../conan-refinement-2026-09-13.md)。
