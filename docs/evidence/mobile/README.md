# 移动端验收证据 · 2026-09-12

本目录图片是 **桌面 Chromium + Metal 的移动视口/触摸模拟**，不是生成图，不是实物手机录屏。页面来自本地候选：小世界 `127.0.0.1:8784/frank_worlds/`，官网 `127.0.0.1:8785/`。未据此声称公开发布。

- 最终浏览器报告：`browser-report.json`，15/15 项通过，未捕获页面脚本错误。
- 门户：390×844、844×390、360×640，无文档横溢出，语言/声音/来访本可见且触摸目标至少 44px。
- 哆啦 A 梦：真实模型启动；390×844 下画布 390×788，844×390 下画布 844×342；真实同时触摸摇杆、镜头、跑步后角色移动约 2.47m，设置面板可打开并点关闭。
- `doraemon-bedroom-day-390.png` / `doraemon-bedroom-night-390.png`：按实际入口加载原完整卧室后，15:30 / 21:00 第一人称视角。7 个窗面光、5 个实用灯；未替换卧室模型和材质质量。
- 官网：三个尺寸；实际触摸视频轮播 03/05 → 04/05，打开/关闭视频，表单输入 16px 并无横溢出。候选在 `work/xgen-mobile-preview`；四个文件已按 SHA 清单回写原 `kan-x/outputs/xgen-site` 并核验一致，原件已备份。公司域名待提供，未另行公开部署。
- 原生壳输入与下载器：`npm run test:mobile` 包含 4 指并发、独立释放、取消/失焦清零、Range 续传、校验和超时检查。
- `bridge-unit.gd` 用 Godot **4.4.1** 在隔离项目验证两 bridge 解析、菜单重复翻译与 item metadata 保留、模拟轴强度和松手清零；不是 Godot 4.7 原生/Web 全场景测试。设置 `XLANDS_REPO` 指向仓库再运行。

本轮发现并修复：手机后台抢载 244.7MB 卧室、时间按钮点击穿到画布、门户通用 label 规则挤坏世界语言栏、设置弹窗被世界导航遮挡、轮播转交 pointer capture 被误判为取消。最终截图在这些修正后重新采集。

详细方法见 [手机端 3D 世界制作与验收](../../mobile-world-method.md)。

## Godot 4.7.2 完整网页包

`native-frog/report.json`、`native-conan/report.json` 为最终导出的实际 WebGL2 浏览器结果，均通过390×844与844×390、三指并发、松手复位及无脚本错误检查。`pack-check.json` 校验全部27个分块、组装字节SHA、损坏重试与失败提示；这些结果独立于最初4.4桥接单测。

本地/缓存下载时间不能代表公网或真手机速度；尚未进行实物iPhone/Android验收。公开版本以网站 `release.json` 为准。

## 公开运行与完整表单检查

[第18版公开检查](public-release18/README.md)记录公网清单、模型包与实际运行。整页截图进一步发现的窄屏来访本问题见[表单修复与触摸验证](guestbook-form/README.md)。两项报告分别保留其真实范围，不能把浏览器视口模拟等同实体手机验收，也不能把输入验证等同留言后端可用。

## 最终门户内触摸菜单

[最终菜单验收](embedded-conan/README.md) 在真实门户 iframe 中验证中文竖屏、日文横屏的自然触摸滚动和环形厨房跳转；青蛙行囊打开时隐藏摇杆，关闭后恢复。`embedded-conan/final/report.json` 5/5 通过。旧根目录截图属于修复前诊断，不计入最终通过证据。
