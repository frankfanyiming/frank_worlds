# 一起共建

1. 开一个 Issue 描述你想改的地点或玩法，附自己的截图/草图。
2. Fork 仓库后从 main 建分支，保持每个 PR 解决一个明确问题。
3. 在 `worlds/doraemon/web` 安装依赖并运行。提交前完成类型检查、`tools/verify-world.mjs` 和构建；玩法或动作修改请补充实际验证记录。
4. 模型使用米为单位，导出 GLB；保留 UV、法线和内嵌贴图。单个网页模型小于 25 MiB，新增建筑确认门洞、楼梯、角色脚底与碰撞一致。
5. 人物保留 Idle / Walk / Run / Wave 动作，不用整体抖动代替骨骼动画；车辆轮子接地、转弯连续，避免车身跟着帧率抖动。
6. 图片、音频与模型必须记录来源、许可和修改者；不要提交密钥、个人文件、抓取漫画/视频、编辑器缓存或 node_modules。
7. 不同世界使用独立目录。大量源资产放 Release，附 SHA-256 和导入说明。

PR 请说明：改变了什么、如何验证、效果截图、资产来源。保留 Frank 和其他贡献者署名；新增贡献也请署名。
